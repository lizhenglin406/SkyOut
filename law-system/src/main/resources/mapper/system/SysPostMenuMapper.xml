<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.law.system.mapper.SysPostMenuMapper">

	<resultMap type="SysPostMenu" id="SysPostMenuResult">
		<result property="postId"     column="post_id"      />
		<result property="menuId"     column="menu_id"      />
	</resultMap>

	<select id="checkPostExistMenu" parameterType="Long" resultType="int">
		select count(1) from sys_post_menu where menu_id = #{menuId}
	</select>

	<delete id="deletePostMenuByPostId" parameterType="Long">
		delete from sys_post_menu where post_id = #{postId}
	</delete>

	<delete id="deletePostMenuByMenuId" parameterType="Long">
		delete from sys_post_menu where menu_id = #{menuId}
	</delete>

	<delete id="deletePostMenu" parameterType="Long">
 		delete from sys_post_menu where post_id in
 		<foreach collection="array" item="postId" open="(" separator="," close=")">
 			#{postId}
        </foreach>
 	</delete>

	<insert id="batchPostMenu">
		insert into sys_post_menu(post_id, menu_id) values
		<foreach item="item" index="index" collection="list" separator=",">
			(#{item.postId},#{item.menuId})
		</foreach>
	</insert>

	<select id="selectMenuPermsByPostId" parameterType="Long" resultType="String">
		select distinct m.perms
		from sys_menu m
			 left join sys_post_menu pm on m.menu_id = pm.menu_id
		where m.status = '0' and pm.post_id = #{postId}
	</select>

	<select id="selectMenuListByPostId" resultType="Long">
		select m.menu_id
		from sys_menu m
            left join sys_post_menu pm on m.menu_id = pm.menu_id
        where pm.post_id = #{postId}
            <if test="menuCheckStrictly">
              and m.menu_id not in (select m.parent_id from sys_menu m inner join sys_post_menu pm on m.menu_id = pm.menu_id and pm.post_id = #{postId})
            </if>
		order by m.parent_id, m.order_num
	</select>

	<select id="selectMenuPermsByUserId" parameterType="Long" resultType="String">
		select distinct m.perms
		from sys_menu m
			 inner join sys_post_menu pm on m.menu_id = pm.menu_id
			 inner join sys_user_post up on pm.post_id = up.post_id
		where m.status = '0' and up.user_id = #{userId}
	</select>

</mapper>
